---
import { getPosts } from "@/lib/collections";

const recentPosts = (await getPosts()).slice(0, 2);
const currentYear = new Date().getFullYear();

const significaLinks = [
  { label: "Home", href: "/" },
  { label: "About", href: "/about" },
  { label: "Services", href: "/services" },
  { label: "Projects", href: "/projects" },
  { label: "Impact", href: "/impact" },
  { label: "Say hello", href: "/say-hello" },
  { label: "Careers", href: "/careers" },
  { label: "Blog", href: "/blog" },
  { label: "Playground", href: "/playground" },
];

const handbookLinks = [
  { label: "Handbook", href: "/handbook" },
  { label: "Playbook", href: "/playbook" },
  { label: "Mission & Values", href: "/mission-and-values" },
  { label: "Culture", href: "/culture" },
  { label: "How we collaborate", href: "/how-we-collaborate" },
];
---

<footer class="site-footer">
  <div class="footer-inner">
    <!-- Top: CTA + navigation columns -->
    <div class="footer-top">
      <div class="footer-cta">
        <span class="footer-logo">Significa®</span>
        <h2 class="footer-headline">We are ready to grow your business. Are you?</h2>
        <div class="footer-email">
          <a href="mailto:newbusiness@significa.co">newbusiness@significa.co</a>
        </div>
      </div>

      <nav class="footer-nav" aria-label="Footer navigation">
        <div class="footer-nav-column">
          <h3>Significa</h3>
          <ul>
            {
              significaLinks.map((link) => (
                <li>
                  <a href={link.href}>{link.label}</a>
                </li>
              ))
            }
          </ul>
        </div>

        <div class="footer-nav-column">
          <h3>Handbook</h3>
          <ul>
            {
              handbookLinks.map((link) => (
                <li>
                  <a href={link.href}>{link.label}</a>
                </li>
              ))
            }
          </ul>
        </div>

        <div class="footer-nav-column">
          <h3>Latest from blog</h3>
          <ul>
            {
              recentPosts.map((post) => (
                <li>
                  <a href={`/blog/${post.id}`}>{post.data.title}</a>
                </li>
              ))
            }
            <li>
              <a href="/blog">See all</a>
            </li>
          </ul>
        </div>
      </nav>
    </div>

    <!-- Bottom: certifications + legal -->
    <div class="footer-bottom">
      <div class="footer-certifications">
        <a href="/b-corp" class="certification-badge certification-badge--link" title="Certified B Corporation">B Corp</a>
        <span class="certification-badge" title="1% for the Planet">1%</span>
      </div>
      <p class="footer-legal">
        &copy; {currentYear} Significa. All rights reserved. · <a href="/legal">Terms & Privacy</a>
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--color-border);
    padding: 4rem var(--gutter) 2rem;
    margin-top: 4rem;
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  /* --------------------------------------------------------
       Top section: CTA + nav columns
       -------------------------------------------------------- */
  .footer-top {
    display: grid;
    grid-template-columns: 1fr 3fr;
    gap: 4rem;
    padding-bottom: 4rem;
  }

  .footer-logo {
    display: block;
    font-weight: var(--weight-medium);
    font-size: var(--text-base);
    margin-bottom: 2rem;
  }

  .footer-headline {
    font-size: var(--text-2xl);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
    margin-bottom: 1.5rem;
    max-width: 14ch;
  }

  .footer-email a {
    display: inline-block;
    font-size: var(--text-sm);
    color: var(--color-text);
    text-decoration: none;
    padding: 0.5rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: 2rem;
    transition: border-color var(--duration-fast) var(--ease-out);
  }

  .footer-email a:hover {
    border-color: var(--color-text);
    color: var(--color-text);
  }

  /* --------------------------------------------------------
       Navigation columns
       -------------------------------------------------------- */
  .footer-nav {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .footer-nav-column h3 {
    font-size: var(--text-sm);
    font-weight: var(--weight-normal);
    color: var(--color-text-muted);
    margin-bottom: 1rem;
  }

  .footer-nav-column ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .footer-nav-column a {
    font-size: var(--text-sm);
    color: var(--color-text);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer-nav-column a:hover {
    color: var(--color-text-muted);
  }

  /* --------------------------------------------------------
       Bottom section: certifications + legal
       -------------------------------------------------------- */
  .footer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 2rem;
    border-top: 1px solid var(--color-border);
  }

  .footer-certifications {
    display: flex;
    gap: 1rem;
  }

  .certification-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    border: 1px solid var(--color-border);
    font-size: 0.625rem;
    font-weight: var(--weight-bold);
    color: var(--color-text);
  }

  .certification-badge--link {
    text-decoration: none;
    transition: border-color var(--duration-fast) var(--ease-out);
  }

  .certification-badge--link:hover {
    border-color: var(--color-text);
    color: var(--color-text);
  }

  .footer-legal {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin: 0;
  }

  .footer-legal a {
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer-legal a:hover {
    color: var(--color-text);
  }

  /* --------------------------------------------------------
       Responsive
       -------------------------------------------------------- */
  @media (max-width: 1024px) {
    .footer-top {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }

  @media (max-width: 640px) {
    .footer-nav {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }
  }
</style>
