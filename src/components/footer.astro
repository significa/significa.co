---
/**
 * Footer
 *
 * Editorial close. Same paper as the page (no separate gray surface).
 * Three-cell top with the pull quote as the visual centerpiece, then
 * a mono nav row, then legal/marks. Contact email gets prominence;
 * the manifesto line provides the moment.
 *
 * No "ready to grow your business" CTA. We close on point of view +
 * direct contact, not on a sales line.
 */

import Egg from "@/components/ui/egg.astro";

const currentYear = new Date().getFullYear();

const navItems = [
  { label: "Work", href: "/projects" },
  { label: "Studio", href: "/about" },
  { label: "Services", href: "/services" },
  { label: "Journal", href: "/blog" },
  { label: "Handbook", href: "/handbook" },
  { label: "Hello", href: "/say-hello" },
];
---

<footer class="site-footer">
  <div class="footer-shell">
    <!-- Top: 3-cell editorial -->
    <div class="footer-top">
      <!-- Note (left) -->
      <div class="footer-cell footer-cell-note">
        <p class="footer-eyebrow">A studio in Porto</p>
        <p class="footer-note">
          Designers and engineers working as one team. We take on complex
          problems for clients who want them solved with care.
        </p>
      </div>

      <!-- Pull quote (centerpiece) -->
      <div class="footer-cell footer-cell-quote">
        <p class="footer-quote"><em>The deciding<br />is the work.</em></p>
        <p class="footer-quote-attr">
          <Egg variant="fill" class="footer-quote-egg" />
          North Star
        </p>
      </div>

      <!-- Contact (right) -->
      <div class="footer-cell footer-cell-contact">
        <p class="footer-eyebrow">Hello</p>
        <p class="footer-contact-line">
          <a href="mailto:newbusiness@significa.co" class="footer-email">newbusiness@significa.co</a>
        </p>
        <dl class="footer-rows">
          <div class="footer-row">
            <dt class="footer-eyebrow">Studio</dt>
            <dd>
              <a
                href="https://maps.app.goo.gl/Y9fLYR12K7iKjYpA9"
                target="_blank"
                rel="noopener noreferrer">Rua de Cândido dos Reis 80, Porto</a
              >
            </dd>
          </div>
          <div class="footer-row">
            <dt class="footer-eyebrow">Currently</dt>
            <dd class="footer-current">designing for healthcare</dd>
          </div>
        </dl>
      </div>
    </div>

    <!-- Mono nav row -->
    <nav class="footer-nav" aria-label="Footer navigation">
      <ul role="list">
        {
          navItems.map((item) => (
            <li>
              <a href={item.href}>{item.label}</a>
            </li>
          ))
        }
      </ul>
    </nav>

    <!-- Legal + marks -->
    <div class="footer-legal">
      <div class="footer-marks">
        <a href="/b-corp" class="footer-mark">B Corp</a>
        <span class="footer-mark">1% for the Planet</span>
      </div>
      <p class="footer-legal-text">
        <Egg variant="fill" class="footer-legal-egg" />
        &copy; {currentYear} Significa <span class="footer-sep">/</span>
        <a href="/legal">Terms &amp; privacy</a>
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--color-rule);
    background: var(--color-paper);
    padding-block: clamp(5rem, 10vw, 9rem) clamp(2rem, 4vw, 4rem);
    margin-top: clamp(4rem, 8vw, 8rem);
  }

  .footer-shell {
    max-width: var(--max-width-wide);
    margin: 0 auto;
    padding-inline: var(--gutter);
  }

  /* ============================================================
     Mono utility — used in eyebrows, nav, marks, legal
     ============================================================ */

  .footer-eyebrow {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-ink-muted);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .footer-sep {
    color: var(--color-ink-faint);
    margin-inline: 0.35em;
  }

  /* ============================================================
     Top: 3-cell editorial. Quote in the middle is the centerpiece.
     ============================================================ */

  .footer-top {
    display: grid;
    grid-template-columns: 1fr 1.2fr 1fr;
    gap: 4rem;
    align-items: start;
    margin-bottom: clamp(4rem, 8vw, 6rem);
  }

  .footer-cell {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    min-width: 0;
  }

  /* Note ---------------------------------------------- */

  .footer-note {
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    color: var(--color-ink-muted);
    margin: 0;
    max-width: 32ch;
  }

  /* Pull quote ---------------------------------------- */

  .footer-cell-quote {
    align-items: flex-start;
  }

  .footer-quote {
    font-family: var(--font-display);
    font-weight: 200;
    font-style: italic;
    font-size: var(--text-4xl);
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tighter);
    color: var(--color-ink);
    margin: 0;
  }

  .footer-quote-attr {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-ink-muted);
    margin: 0.5rem 0 0;
    display: inline-flex;
    align-items: center;
    gap: 0.5em;
  }

  .footer-quote-egg {
    color: var(--color-accent);
    height: 0.85em;
  }

  /* Contact ------------------------------------------- */

  .footer-cell-contact {
    gap: 1.5rem;
  }

  .footer-contact-line {
    margin: 0;
  }

  .footer-email {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: var(--text-xl);
    line-height: var(--leading-snug);
    color: var(--color-ink);
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer-email:hover {
    color: var(--color-accent);
  }

  .footer-rows {
    margin: 0.75rem 0 0;
    display: grid;
    gap: 1rem;
  }

  .footer-row {
    display: grid;
    gap: 0.2rem;
  }

  .footer-row dt {
    margin: 0;
  }

  .footer-row dd {
    margin: 0;
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    color: var(--color-ink);
  }

  .footer-row dd a {
    color: inherit;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer-row dd a:hover {
    color: var(--color-accent);
  }

  .footer-current {
    color: var(--color-accent) !important;
  }

  /* ============================================================
     Mono nav row
     ============================================================ */

  .footer-nav {
    margin-bottom: clamp(2rem, 5vw, 3.5rem);
    padding-top: 2rem;
    border-top: 1px solid var(--color-rule);
  }

  .footer-nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
  }

  .footer-nav a {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-ink-muted);
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer-nav a:hover {
    color: var(--color-ink);
  }

  /* ============================================================
     Legal row — marks left, copyright right
     ============================================================ */

  .footer-legal {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .footer-marks {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .footer-mark {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-ink-muted);
    transition: color var(--duration-fast) var(--ease-out);
  }

  a.footer-mark:hover {
    color: var(--color-ink);
  }

  .footer-legal-text {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-ink-muted);
    margin: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.45em;
  }

  .footer-legal-text a {
    color: inherit;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer-legal-text a:hover {
    color: var(--color-ink);
  }

  .footer-legal-egg {
    height: 0.85em;
    color: var(--color-accent);
  }

  /* ============================================================
     Responsive
     ============================================================ */

  @media (max-width: 980px) {
    .footer-top {
      grid-template-columns: 1fr 1fr;
      gap: 3rem;
    }

    .footer-cell-quote {
      grid-column: 1 / -1;
      order: -1;
      align-items: flex-start;
      margin-bottom: 1rem;
    }
  }

  @media (max-width: 640px) {
    .footer-top {
      grid-template-columns: 1fr;
    }

    .footer-cell-quote {
      grid-column: auto;
    }

    .footer-quote {
      font-size: var(--text-3xl);
    }

    .footer-legal {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.25rem;
    }
  }
</style>
