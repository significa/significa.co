---
import BaseLayout from "@/layouts/base.astro";
import { getClients, getProjects, getAwards, getTestimonials, getPosts, getPlayground } from "@/lib/collections";
import { cdnUrl } from "@/lib/cdn";

const featuredProjects = (await getProjects()).slice(0, 3);
const clients = await getClients();
const awards = await getAwards();
const testimonials = await getTestimonials();
const recentPost = (await getPosts())[0];
const recentPlayground = (await getPlayground())[0];

const pillars = [
  {
    id: "think",
    label: "Think.",
    description:
      "We ask the right questions to help you iterate, uncover critical challenges, and define a clear path forward. From product strategy to UX audits and technical planning, we work with you to map out what to build and why.",
    deliverables: ["Product Definition", "Migrations", "Operations"],
  },
  {
    id: "design",
    label: "Design.",
    description:
      "We transform insights into impact through design. From user interface design to brands and Design Systems, our work is built to function beautifully, scale purposefully, and guide users intentionally. Design decisions are business decisions, so our developers are involved early to align UI with technical possibilities.",
    deliverables: ["UI / UX Design", "Design Systems", "Branding"],
  },
  {
    id: "develop",
    label: "Develop.",
    description:
      "We combine critical thinking, agility, and curiosity to write code that goes beyond functionality, turning solid products into seamless, memorable experiences. Our custom development services follow two principles: clarity and speed.",
    deliverables: ["IT Architecture", "Headless", "Dev Ops", "Shopify"],
  },
  {
    id: "launch",
    label: "Launch.",
    description:
      "We launch with confidence. We build software that's robust, easy to maintain, and ready to grow. Frameworks are set up for efficiency and consistency, while clear documentation and onboarding ensure smooth handover to your team and partners.",
    deliverables: ["Quality Assurance", "Product Governance", "On-call Maintenance"],
  },
  {
    id: "scale",
    label: "Scale.",
    description:
      "Growth is a continuous process. We track performance, monitor user behaviour, and surface friction points to help you evolve based on evidence, not assumptions. From new features to technical improvements, we keep your product moving forward with momentum.",
    deliverables: ["Performance Insights", "Optimisations", "Retainers"],
  },
] as const;

const productTypes = [
  { label: "E-commerce", description: "Shopify online shops to sell and manage all your products." },
  { label: "Websites", description: "Corporate or marketing websites." },
  { label: "Desktop Apps", description: "Apps that run natively on computers." },
  { label: "Web Apps", description: "Apps that run on web browsers." },
  { label: "Watch & TV Apps", description: "Apps that run natively on smart watches and TVs." },
  { label: "Mobile Apps", description: "Apps that run natively on Android and iOS phones." },
];

const industries = [
  "Health & Wellness",
  "Sports & Entertainment",
  "Finance",
  "Hospitality & Real Estate",
  "HR & Comms",
  "Commerce",
  "Sustainability",
  "Education",
  "Mobility",
  "Culture & Community",
];
---

<BaseLayout
  title="What we do — Significa"
  description="Custom design and development services built to grow your business. Strategy-driven, design-led, code-savvy — from product strategy to launch and beyond."
>
  <!-- ============================================================
       HERO
       ============================================================ -->
  <section class="hero">
    <div class="hero-inner">
      <h1>
        Custom <strong>design and development&nbsp;services</strong> built to grow your business.
      </h1>
      <p class="hero-sub">Strategy-driven. Design-led. Code-savvy.</p>
      <p class="hero-description">
        We combine strategy, design and engineering to build bespoke digital products that are intuitive, user-centred, and
        built to last. Our process is collaborative, rooted in trust, curiosity and transparency.
      </p>
    </div>
  </section>

  <!-- ============================================================
       STATS STRIP
       ============================================================ -->
  <section class="stats-strip">
    <div class="stats-strip-inner">
      <dl class="stats-grid">
        <div class="stat-item">
          <dd class="stat-value">10+</dd>
          <dt class="stat-label">Years of experience</dt>
        </div>
        <div class="stat-item">
          <dd class="stat-value">100+</dd>
          <dt class="stat-label">Projects delivered</dt>
        </div>
        <div class="stat-item">
          <dd class="stat-value">20+</dd>
          <dt class="stat-label">International awards</dt>
        </div>
        <div class="stat-item">
          <dd class="stat-value">84.7%</dd>
          <dt class="stat-label">Customer satisfaction</dt>
        </div>
      </dl>
    </div>
  </section>

  <!-- ============================================================
       SERVICES INTRO
       ============================================================ -->
  <section class="services-intro">
    <div class="services-intro-inner">
      <p class="eyebrow">Our services</p>
      <div class="services-intro-layout">
        <h2 class="services-intro-heading">Your go-to partner to think, design, develop and scale.</h2>
        <div class="services-intro-aside">
          <p>
            Backed by 10+ years of experience, 20+ international awards and a team of 30+ experts, with 100+ projects delivered,
            we are your go-to partner to build digital experiences that are intuitive, user-centred, and built to drive lasting
            growth.
          </p>
          <a href="/say-hello" class="cta-button">Get a quote</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================================================
       SERVICE PILLARS
       ============================================================ -->
  <section class="pillars">
    <div class="pillars-inner">
      {
        pillars.map((pillar, i) => (
          <article class="pillar" id={pillar.id}>
            <div class="pillar-header">
              <span class="pillar-number" aria-hidden="true">
                0{i + 1}
              </span>
              <h2 class="pillar-title">{pillar.label}</h2>
            </div>
            <div class="pillar-body">
              <p class="pillar-description">{pillar.description}</p>
              <ul class="pillar-deliverables">
                {pillar.deliverables.map((d) => (
                  <li>{d}</li>
                ))}
              </ul>
            </div>
          </article>
        ))
      }
    </div>
  </section>

  <!-- ============================================================
       TESTIMONIAL
       ============================================================ -->
  {
    testimonials[0] && (
      <section class="testimonial-section">
        <div class="testimonial-section-inner">
          <blockquote class="testimonial">
            <p class="testimonial-quote">&ldquo;{testimonials[0].data.quote}&rdquo;</p>
            <footer class="testimonial-author">
              <cite>
                <strong>{testimonials[0].data.author}</strong>
                <span>
                  {testimonials[0].data.role} at {testimonials[0].data.company}
                </span>
              </cite>
            </footer>
          </blockquote>
        </div>
      </section>
    )
  }

  <!-- ============================================================
       HOW WE WORK
       ============================================================ -->
  <section class="how-we-work">
    <div class="how-we-work-inner">
      <div class="how-we-work-text">
        <p class="eyebrow">How we work</p>
        <h2>Collaborative, hands-on, and jargon-free.</h2>
        <p>
          Whether you're re-platforming an e-commerce business, launching a new mobile app, or refining a digital product, we
          bring clarity, focus, and intent to our process. There's no room for micromanaging or technical jargon — just good
          work, done together.
        </p>
        <a href="/handbook/how-we-build-software" class="link-arrow">Read our playbook →</a>
      </div>
      <nav class="how-we-work-steps" aria-label="Service phases">
        {
          pillars.map((pillar) => (
            <a href={`#${pillar.id}`} class="step-item">
              <span class="step-label">{pillar.label}</span>
              <span class="step-arrow" aria-hidden="true">
                ↗
              </span>
            </a>
          ))
        }
      </nav>
    </div>
  </section>

  <!-- ============================================================
       DELIVERABLES
       ============================================================ -->
  <section class="deliverables">
    <div class="deliverables-inner">
      <div class="deliverables-header">
        <p class="eyebrow">What we build</p>
        <h2>Every product type, end-to-end.</h2>
        <p class="deliverables-sub">Including but not limited to.</p>
      </div>
      <ul class="deliverables-grid">
        {
          productTypes.map((type) => (
            <li class="deliverable-item">
              <h3 class="deliverable-label">{type.label}</h3>
              <p class="deliverable-description">{type.description}</p>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <!-- ============================================================
       INDUSTRIES
       ============================================================ -->
  <section class="industries">
    <div class="industries-inner">
      <div class="industries-header">
        <p class="eyebrow">Industries</p>
        <h2>Industries we've shaped.</h2>
        <p>
          From regulated finance platforms to consumer mobile apps, we've designed and built products across a wide range of
          industries — bringing the same depth of thinking to each one.
        </p>
      </div>
      <ul class="industries-list">
        {
          industries.map((industry) => (
            <li class="industry-item">
              <span>{industry}</span>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <!-- ============================================================
       SELECTED WORK
       ============================================================ -->
  {
    featuredProjects.length > 0 && (
      <section class="featured-work">
        <div class="featured-work-inner">
          <div class="featured-work-header">
            <p class="eyebrow">Selected work</p>
            <a href="/projects" class="link-arrow">
              All projects →
            </a>
          </div>
          <ul class="work-list">
            {featuredProjects.map((project) => (
              <li class="work-item">
                <a href={`/projects/${project.id}`}>
                  {project.data.thumbnail && (
                    <div class="work-item-image">
                      <img
                        src={cdnUrl(project.data.thumbnail, { width: 120, quality: 80 })}
                        alt={project.data.title}
                        width="120"
                        height="90"
                        loading="lazy"
                        decoding="async"
                      />
                    </div>
                  )}
                  <div class="work-item-content">
                    <h3 class="work-item-title">{project.data.client ?? project.data.title}</h3>
                    {project.data.tagline && <p class="work-item-tagline">{project.data.tagline}</p>}
                  </div>
                  <ul class="work-item-tags" aria-label="Tags">
                    {project.data.tags.slice(0, 3).map((tag) => (
                      <li class="work-tag">{tag}</li>
                    ))}
                  </ul>
                  <span class="work-item-arrow" aria-hidden="true">
                    →
                  </span>
                </a>
              </li>
            ))}
          </ul>
        </div>
      </section>
    )
  }

  <!-- ============================================================
       FROM OUR TEAM (blog + playground crosssell)
       ============================================================ -->
  {
    (recentPost || recentPlayground) && (
      <section class="crosssell">
        <div class="crosssell-inner">
          <p class="eyebrow">From our team</p>
          <div class="crosssell-grid">
            {recentPost && (
              <a href={`/blog/${recentPost.id}`} class="crosssell-card">
                {recentPost.data.thumbnail && (
                  <div class="crosssell-image">
                    <img
                      src={cdnUrl(recentPost.data.thumbnail, { width: 600, quality: 80 })}
                      alt={recentPost.data.title}
                      width="600"
                      height="400"
                      loading="lazy"
                      decoding="async"
                    />
                  </div>
                )}
                <div class="crosssell-content">
                  <span class="crosssell-type">Blog</span>
                  <h3 class="crosssell-title">{recentPost.data.title}</h3>
                  {recentPost.data.description && <p class="crosssell-description">{recentPost.data.description}</p>}
                </div>
              </a>
            )}
            {recentPlayground && (
              <a href={`/playground/${recentPlayground.id}`} class="crosssell-card">
                {recentPlayground.data.thumbnail && (
                  <div class="crosssell-image">
                    <img
                      src={cdnUrl(recentPlayground.data.thumbnail, { width: 600, quality: 80 })}
                      alt={recentPlayground.data.title}
                      width="600"
                      height="400"
                      loading="lazy"
                      decoding="async"
                    />
                  </div>
                )}
                <div class="crosssell-content">
                  <span class="crosssell-type">Playground</span>
                  <h3 class="crosssell-title">{recentPlayground.data.title}</h3>
                  <p class="crosssell-description">{recentPlayground.data.description}</p>
                </div>
              </a>
            )}
          </div>
        </div>
      </section>
    )
  }

  <!-- ============================================================
       AWARDS
       ============================================================ -->
  {
    awards.length > 0 && (
      <section class="awards">
        <div class="awards-inner">
          <div class="awards-header">
            <p class="eyebrow">Recognition</p>
            <h2>International recognition.</h2>
            <p class="awards-sub">+20 awards across design, development, and innovation.</p>
          </div>
          <ul class="awards-list">
            {awards.map((a) => (
              <li class="award-item">
                {a.url ? (
                  <a href={a.url} target="_blank" rel="noopener noreferrer">
                    <span class="award-name">{a.award}</span>
                    <span class="award-year">{a.year}</span>
                    <span class="award-project">{a.project?.data.client ?? a.project?.data.title}</span>
                  </a>
                ) : (
                  <span class="award-item-inner">
                    <span class="award-name">{a.award}</span>
                    <span class="award-year">{a.year}</span>
                    <span class="award-project">{a.project?.data.client ?? a.project?.data.title}</span>
                  </span>
                )}
              </li>
            ))}
          </ul>
        </div>
      </section>
    )
  }

  <!-- ============================================================
       CLIENTS
       ============================================================ -->
  {
    clients.length > 0 && (
      <section class="clients">
        <div class="clients-inner">
          <p class="eyebrow clients-eyebrow">Companies we've worked with</p>
          <ul class="clients-list">
            {clients.map((client) => (
              <li>
                {client.data.url ? (
                  <a href={client.data.url} target="_blank" rel="noopener noreferrer" title={client.data.name}>
                    <span class="client-name">{client.data.name}</span>
                  </a>
                ) : (
                  <span class="client-name">{client.data.name}</span>
                )}
              </li>
            ))}
          </ul>
        </div>
      </section>
    )
  }

  <!-- ============================================================
       FINAL CTA
       ============================================================ -->
  <section class="final-cta">
    <div class="final-cta-inner">
      <h2>Ready to build something great together?</h2>
      <p>Tell us about your project. We'll figure out whether we're the right fit — no pressure, no jargon.</p>
      <a href="/work-with-us" class="cta-button-large">Get in touch</a>
    </div>
  </section>
</BaseLayout>

<style>
  /* ============================================================
     Design tokens (local shorthands)
     ============================================================ */

  /*
     Section rhythm:
       - Every section has a symmetric block-padding of --section-py
       - "Wide" sections (alt background) use --section-py-wide
       - Inner wrappers constrain content to --max-width or --max-width-narrow
  */

  /* ============================================================
     Shared utilities
     ============================================================ */

  /* Eyebrow label that sits above section headings */
  .eyebrow {
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
    margin-bottom: 1.25rem;
  }

  /* Small inline arrow link */
  .link-arrow {
    display: inline-block;
    font-size: var(--text-sm);
    font-weight: var(--weight-medium);
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }
  .link-arrow:hover {
    color: var(--color-text);
  }

  /* Filled CTA button */
  .cta-button {
    display: inline-block;
    padding: 0.8125rem 1.75rem;
    font-size: var(--text-base);
    font-weight: var(--weight-semibold);
    text-decoration: none;
    border-radius: 0.375rem;
    background: var(--color-text);
    color: var(--color-surface);
    transition:
      opacity var(--duration-fast) var(--ease-out),
      transform var(--duration-fast) var(--ease-out);
  }
  .cta-button:hover {
    opacity: 0.82;
    transform: translateY(-1px);
  }

  /* Large filled CTA (for final CTA section) */
  .cta-button-large {
    display: inline-block;
    padding: 1rem 2.5rem;
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    text-decoration: none;
    border-radius: 0.375rem;
    background: var(--color-text);
    color: var(--color-surface);
    transition:
      opacity var(--duration-fast) var(--ease-out),
      transform var(--duration-fast) var(--ease-out);
  }
  .cta-button-large:hover {
    opacity: 0.82;
    transform: translateY(-1px);
  }

  /* ============================================================
     HERO
     ============================================================ */
  .hero {
    padding: 9rem var(--gutter) 6rem;
  }

  .hero-inner {
    max-width: var(--max-width-narrow);
    margin: 0 auto;
  }

  .hero h1 {
    font-size: clamp(2rem, 1.25rem + 3vw, 3.25rem);
    font-weight: var(--weight-normal);
    line-height: var(--leading-tight);
    color: var(--color-text);
    margin-bottom: 2.5rem;
  }

  .hero h1 strong {
    font-weight: var(--weight-bold);
  }

  /* Thin rule between h1 and the two-line sub-copy */
  .hero-sub {
    font-size: var(--text-base);
    font-weight: var(--weight-semibold);
    color: var(--color-text);
    padding-top: 1.25rem;
    border-top: 1px solid var(--color-border);
    margin-bottom: 1rem;
  }

  .hero-description {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    max-width: 58ch;
  }

  /* ============================================================
     STATS STRIP
     ============================================================ */
  .stats-strip {
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface-alt);
  }

  .stats-strip-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--gutter);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .stat-item {
    padding: 2.5rem 2rem;
    border-right: 1px solid var(--color-border);
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .stat-item:last-child {
    border-right: none;
  }

  .stat-value {
    font-size: clamp(1.75rem, 1.25rem + 2vw, 2.75rem);
    font-weight: var(--weight-bold);
    line-height: 1;
    color: var(--color-text);
  }

  .stat-label {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: var(--leading-normal);
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .stat-item:nth-child(2) {
      border-right: none;
    }
    .stat-item:nth-child(3),
    .stat-item:nth-child(4) {
      border-top: 1px solid var(--color-border);
    }
  }

  @media (max-width: 480px) {
    .stats-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  /* ============================================================
     SERVICES INTRO
     ============================================================ */
  .services-intro {
    padding: 7rem var(--gutter);
  }

  .services-intro-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .services-intro-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: start;
  }

  .services-intro-heading {
    font-size: clamp(1.75rem, 1.25rem + 2vw, 2.5rem);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
  }

  .services-intro-aside p {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    margin-bottom: 2.5rem;
  }

  @media (max-width: 900px) {
    .services-intro-layout {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
  }

  /* ============================================================
     SERVICE PILLARS
     ============================================================ */
  .pillars {
    background: var(--color-surface-alt);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    padding: 0 var(--gutter);
  }

  .pillars-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .pillar {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 5rem;
    padding: 5rem 0;
    border-bottom: 1px solid var(--color-border);
    align-items: start;
  }

  .pillar:first-child {
    border-top: none;
  }

  .pillar:last-child {
    border-bottom: none;
  }

  .pillar-header {
    position: sticky;
    top: 3rem;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }

  .pillar-number {
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
    font-variant-numeric: tabular-nums;
  }

  .pillar-title {
    font-size: clamp(2rem, 1.5rem + 2vw, 3rem);
    font-weight: var(--weight-bold);
    line-height: 1;
    color: var(--color-text);
  }

  .pillar-body {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
    padding-top: 0.25rem; /* optical alignment with the sticky title */
  }

  .pillar-description {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    max-width: 60ch;
  }

  .pillar-deliverables {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    list-style: none;
  }

  .pillar-deliverables li {
    padding: 0.4375rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: 2rem;
    font-size: var(--text-sm);
    font-weight: var(--weight-medium);
    color: var(--color-text);
    background: var(--color-surface);
    transition: border-color var(--duration-fast) var(--ease-out);
  }

  .pillar-deliverables li:hover {
    border-color: var(--color-text);
  }

  @media (max-width: 860px) {
    .pillar {
      grid-template-columns: 1fr;
      gap: 1.75rem;
      padding: 3.5rem 0;
    }

    .pillar-header {
      position: static;
      flex-direction: row;
      align-items: baseline;
      gap: 0.875rem;
    }

    .pillar-title {
      font-size: var(--text-2xl);
    }
  }

  /* ============================================================
     TESTIMONIAL
     ============================================================ */
  .testimonial-section {
    padding: 7rem var(--gutter);
  }

  .testimonial-section-inner {
    max-width: 44rem;
    margin: 0 auto;
    text-align: center;
  }

  .testimonial-quote {
    font-size: clamp(1.25rem, 1rem + 1vw, 1.75rem);
    font-weight: var(--weight-normal);
    line-height: var(--leading-relaxed);
    color: var(--color-text);
    margin-bottom: 2.5rem;
  }

  .testimonial-author cite {
    font-style: normal;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
  }

  .testimonial-author strong {
    font-size: var(--text-base);
    font-weight: var(--weight-semibold);
    color: var(--color-text);
  }

  .testimonial-author span {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  /* ============================================================
     HOW WE WORK
     ============================================================ */
  .how-we-work {
    background: var(--color-surface-alt);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    padding: 7rem var(--gutter);
  }

  .how-we-work-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: start;
  }

  .how-we-work-text h2 {
    font-size: clamp(1.5rem, 1.25rem + 1.5vw, 2.25rem);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
    margin-bottom: 1.25rem;
  }

  .how-we-work-text p {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    margin-bottom: 2rem;
    max-width: 50ch;
  }

  .how-we-work-steps {
    display: flex;
    flex-direction: column;
    border-top: 1px solid var(--color-border);
  }

  .step-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.375rem 0;
    border-bottom: 1px solid var(--color-border);
    text-decoration: none;
    color: var(--color-text-muted);
    transition: color var(--duration-fast) var(--ease-out);
  }

  .step-item:hover {
    color: var(--color-text);
  }

  .step-label {
    font-size: var(--text-xl);
    font-weight: var(--weight-semibold);
  }

  .step-arrow {
    font-size: var(--text-sm);
    opacity: 0;
    transform: translateX(-4px);
    transition:
      opacity var(--duration-fast) var(--ease-out),
      transform var(--duration-fast) var(--ease-out);
  }

  .step-item:hover .step-arrow {
    opacity: 1;
    transform: translateX(0);
  }

  @media (max-width: 860px) {
    .how-we-work-inner {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }

  /* ============================================================
     DELIVERABLES
     ============================================================ */
  .deliverables {
    padding: 7rem var(--gutter);
  }

  .deliverables-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .deliverables-header {
    margin-bottom: 3.5rem;
    max-width: var(--max-width-narrow);
  }

  .deliverables-header h2 {
    font-size: clamp(1.5rem, 1.25rem + 1.5vw, 2.25rem);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
    margin-bottom: 0.5rem;
  }

  .deliverables-sub {
    font-size: var(--text-base);
    color: var(--color-text-muted);
  }

  /* Border-gap grid — 1px gaps achieved with background on the wrapper */
  .deliverables-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    list-style: none;
    background: var(--color-border);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    overflow: hidden;
  }

  .deliverable-item {
    background: var(--color-surface);
    padding: 2.25rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    transition: background var(--duration-fast) var(--ease-out);
  }

  .deliverable-item:hover {
    background: var(--color-surface-alt);
  }

  .deliverable-label {
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    color: var(--color-text);
  }

  .deliverable-description {
    font-size: var(--text-sm);
    line-height: var(--leading-normal);
    color: var(--color-text-muted);
  }

  @media (max-width: 860px) {
    .deliverables-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .deliverables-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ============================================================
     INDUSTRIES
     ============================================================ */
  .industries {
    background: var(--color-surface-alt);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    padding: 7rem var(--gutter);
  }

  .industries-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 5rem;
    align-items: start;
  }

  .industries-header {
    position: sticky;
    top: 3rem;
  }

  .industries-header h2 {
    font-size: clamp(1.5rem, 1.25rem + 1.5vw, 2.25rem);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
    margin-bottom: 1rem;
  }

  .industries-header p {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    max-width: 32ch;
  }

  .industries-list {
    list-style: none;
    border-top: 1px solid var(--color-border);
  }

  .industry-item {
    border-bottom: 1px solid var(--color-border);
    padding: 1.25rem 0;
    font-size: var(--text-xl);
    font-weight: var(--weight-medium);
    color: var(--color-text-muted);
    cursor: default;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .industry-item:hover {
    color: var(--color-text);
  }

  @media (max-width: 860px) {
    .industries-inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .industries-header {
      position: static;
    }

    .industry-item {
      font-size: var(--text-lg);
    }
  }

  /* ============================================================
     SELECTED WORK
     ============================================================ */
  .featured-work {
    padding: 7rem var(--gutter);
  }

  .featured-work-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .featured-work-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.5rem; /* eyebrow sits above the list, close to it */
  }

  .work-list {
    list-style: none;
    border-top: 1px solid var(--color-border);
    margin-top: 2rem;
  }

  .work-item a {
    display: grid;
    grid-template-columns: auto 1fr auto auto;
    align-items: center;
    gap: 1.75rem;
    padding: 1.625rem 0;
    border-bottom: 1px solid var(--color-border);
    text-decoration: none;
    color: var(--color-text);
    transition: opacity var(--duration-fast) var(--ease-out);
  }

  .work-item a:hover {
    opacity: 0.65;
  }

  .work-item-image {
    flex-shrink: 0;
    width: 90px;
    height: 68px;
    border-radius: 0.25rem;
    overflow: hidden;
    background: var(--color-surface-alt);
  }

  .work-item-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .work-item-content {
    min-width: 0;
  }

  .work-item-title {
    font-size: var(--text-xl);
    font-weight: var(--weight-semibold);
    line-height: var(--leading-tight);
  }

  .work-item-tagline {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    margin-top: 0.25rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .work-item-tags {
    display: flex;
    gap: 0.5rem;
    list-style: none;
    flex-shrink: 0;
  }

  .work-tag {
    font-size: var(--text-xs);
    font-weight: var(--weight-medium);
    color: var(--color-text-muted);
    padding: 0.3125rem 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 2rem;
    background: var(--color-surface-alt);
    white-space: nowrap;
  }

  .work-item-arrow {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    flex-shrink: 0;
    transition: transform var(--duration-fast) var(--ease-out);
  }

  .work-item a:hover .work-item-arrow {
    transform: translateX(3px);
  }

  @media (max-width: 860px) {
    .work-item-tags {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .work-item a {
      grid-template-columns: 1fr auto;
    }

    .work-item-image {
      display: none;
    }
  }

  /* ============================================================
     CROSSSELL (blog + playground)
     ============================================================ */
  .crosssell {
    background: var(--color-surface-alt);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    padding: 7rem var(--gutter);
  }

  .crosssell-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .crosssell-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 1.5rem;
    margin-top: 2.5rem;
  }

  .crosssell-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: var(--color-text);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    overflow: hidden;
    background: var(--color-surface);
    transition: border-color var(--duration-fast) var(--ease-out);
  }

  .crosssell-card:hover {
    border-color: var(--color-text);
  }

  .crosssell-image {
    overflow: hidden;
    background: var(--color-surface-alt);
  }

  .crosssell-image img {
    width: 100%;
    height: 220px;
    object-fit: cover;
    display: block;
    transition: transform var(--duration-slow) var(--ease-out);
  }

  .crosssell-card:hover .crosssell-image img {
    transform: scale(1.04);
  }

  .crosssell-content {
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
    flex: 1;
  }

  .crosssell-type {
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
  }

  .crosssell-title {
    font-size: var(--text-xl);
    font-weight: var(--weight-semibold);
    line-height: var(--leading-tight);
  }

  .crosssell-description {
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    color: var(--color-text-muted);
  }

  /* ============================================================
     AWARDS
     ============================================================ */
  .awards {
    padding: 7rem var(--gutter);
  }

  .awards-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 5rem;
    align-items: start;
  }

  .awards-header {
    position: sticky;
    top: 3rem;
  }

  .awards-header h2 {
    font-size: clamp(1.5rem, 1.25rem + 1.5vw, 2.25rem);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
    margin-bottom: 0.625rem;
  }

  .awards-sub {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    max-width: 28ch;
  }

  .awards-list {
    list-style: none;
    border-top: 1px solid var(--color-border);
  }

  .award-item {
    border-bottom: 1px solid var(--color-border);
  }

  .award-item a,
  .award-item-inner {
    display: grid;
    grid-template-columns: 1fr auto auto;
    gap: 2rem;
    align-items: center;
    padding: 1rem 0;
    text-decoration: none;
    color: var(--color-text);
    transition: opacity var(--duration-fast) var(--ease-out);
  }

  .award-item a:hover {
    opacity: 0.55;
  }

  .award-name {
    font-size: var(--text-base);
    font-weight: var(--weight-medium);
  }

  .award-year {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    font-variant-numeric: tabular-nums;
  }

  .award-project {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    text-align: right;
  }

  @media (max-width: 1024px) {
    .awards-inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .awards-header {
      position: static;
    }
  }

  @media (max-width: 600px) {
    .award-item a,
    .award-item-inner {
      grid-template-columns: 1fr auto;
      gap: 1rem;
    }

    .award-project {
      grid-column: 1 / -1;
      text-align: left;
    }
  }

  /* ============================================================
     CLIENTS
     ============================================================ */
  .clients {
    background: var(--color-surface-alt);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    padding: 4rem var(--gutter);
  }

  .clients-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .clients-eyebrow {
    text-align: center;
    margin-bottom: 2rem;
  }

  .clients-list {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 2rem 3rem;
    list-style: none;
  }

  .clients-list li a {
    color: var(--color-text);
    text-decoration: none;
    opacity: 0.45;
    transition: opacity var(--duration-fast) var(--ease-out);
  }

  .clients-list li a:hover {
    opacity: 1;
  }

  .clients-list li span {
    opacity: 0.45;
  }

  .client-name {
    font-size: var(--text-sm);
    font-weight: var(--weight-semibold);
    white-space: nowrap;
  }

  /* ============================================================
     FINAL CTA
     ============================================================ */
  .final-cta {
    padding: 9rem var(--gutter);
  }

  .final-cta-inner {
    max-width: 36rem;
    margin: 0 auto;
    text-align: center;
  }

  .final-cta h2 {
    font-size: clamp(1.75rem, 1.25rem + 2vw, 2.75rem);
    font-weight: var(--weight-bold);
    line-height: var(--leading-tight);
    margin-bottom: 1.25rem;
  }

  .final-cta p {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    margin-bottom: 2.5rem;
  }
</style>
