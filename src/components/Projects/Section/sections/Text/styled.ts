import styled from '@theme'

import { Title as BaseTitle, ArrowLink as BaseArrowLink } from '../../../../UI'

export const Wrapper = styled.div`
  padding: 5em 0;
`

export const Title = styled(BaseTitle)`
  margin-bottom: 0.5em;
`

export const ArrowLink = styled(BaseArrowLink)`
  margin-top: 2.5em;
`
